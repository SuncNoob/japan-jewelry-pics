# Agent instructions (Codex on 无影 Computer)

Each Agent Computer must run **Codex with this machine's Wuying gateway token**. The session should appear in the 无影 console.

For jewelry crawl tasks:

1. Use **browser-use** (real browser), not a silent HTTP scrape.
2. Open the official brand URL, then click into product detail pages (子页面).
3. Save real jewelry photos to `pics/<brand>/`.
4. Stay on allow-listed hosts only. Do not invent images.
